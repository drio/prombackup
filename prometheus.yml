global:
  scrape_interval:     5s
  evaluation_interval: 15s

rule_files:
  # - "first.rules"
  # - "second.rules"
  
scrape_configs:
  - job_name: 'drio'
    static_configs:
      - targets: ['localhost:8080']
